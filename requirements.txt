# ============================================================
# Algo Bot — Python dependencies
# Tested on Python 3.11
# ============================================================

# --- Core data + numerics ---
pandas>=2.2
numpy>=1.26,<2.0           # 2.x breaks numba+other deps; pin for now
polars>=0.20
pyarrow>=15.0
scipy>=1.13
numba>=0.59

# --- Indian market data (free) ---
yfinance>=0.2.40
nsepython>=2.95
pandas-datareader>=0.10

# --- Brokers ---
kiteconnect>=4.2           # Zerodha Kite Connect

# --- Backtesting ---
vectorbt>=0.26
backtesting>=0.3.3

# --- Stats + ML ---
statsmodels>=0.14
scikit-learn>=1.4
lightgbm>=4.3

# --- Sentiment (FinBERT runs locally, no API key) ---
transformers>=4.40
torch>=2.2
vaderSentiment>=3.3.2      # rule-based fallback / sanity check

# --- Web scraping (sentiment) ---
httpx>=0.27
beautifulsoup4>=4.12
feedparser>=6.0
lxml>=5.2
# playwright>=1.43         # uncomment if JS-heavy sites need rendering

# --- Storage ---
sqlalchemy>=2.0
duckdb>=0.10

# --- Scheduling ---
apscheduler>=3.10

# --- Dashboard + Plotting ---
streamlit>=1.34
plotly>=5.20
matplotlib>=3.8

# --- Alerts ---
python-telegram-bot>=21.1

# --- Config + Logging ---
pydantic>=2.7
pydantic-settings>=2.2
pyyaml>=6.0
structlog>=24.1
loguru>=0.7

# --- Dev tools ---
pytest>=8.2
pytest-cov>=5.0
hypothesis>=6.100
ruff>=0.4
mypy>=1.10
pre-commit>=3.7
ipykernel>=6.29
jupyter>=1.0

# --- Optional LLM (commented out — user opted for FinBERT-only) ---
# anthropic>=0.30
# openai>=1.30
